{
  "builders": [
    {
      "type": "virtualbox-iso",
      "name": "virtualbox",
      "vm_name": "{{ user `hostname` }}",

      "format": "ova",
      "guest_os_type": "Ubuntu_64",
      "guest_additions_mode": "attach",
      "disk_size": 40960,
      "hard_drive_interface": "sata",
      "iso_checksum": "{{ user `iso_checksum` }}",
      "iso_checksum_url": "{{ user `iso_checksum_url` }}",
      "iso_checksum_type":  "{{ user `iso_checksum_type` }}",
      "iso_url":  "{{ user `iso_url` }}",
      "http_directory": "{{ template_dir }}",
      "ssh_username": "op",
      "ssh_password": "op",
      "ssh_wait_timeout": "40m",
      "shutdown_command": "echo 'op' | sudo -S halt -p",
      "headless": "{{ user `headless` }}",
      "boot_wait": "5s",
      "boot_command": [
        "<esc><esc><enter><wait>",
        "/install/vmlinuz ",
        "auto-install/enable=true ",
        "auto url=http://{{ .HTTPIP }}:{{ .HTTPPort }}/{{ user `codename` }}.cfg ",
        "hostname={{ .Name }} ",
        "debconf/frontend=noninteractive ",
        "keyboard-configuration/modelcode=SKIP keyboard-configuration/layout=USA ",
        "keyboard-configuration/variant=USA console-setup/ask_detect=false ",
        "passwd/user-fullname=Op ",
        "passwd/username=op ",
        "passwd/user-password=op ",
        "passwd/user-password-again=op ",
        "initrd=/install/initrd.gz -- <enter>"
      ],
      "vboxmanage": [
        ["modifyvm", "{{ .Name }}", "--memory", "1024"],
        ["modifyvm", "{{ .Name }}", "--cpus", "1"     ],
        ["modifyvm", "{{ .Name }}", "--vram", "16"    ],
        ["modifyvm", "{{ .Name }}", "--audio", "none" ],
        ["modifyvm", "{{ .Name }}", "--mouse", "ps2"  ]
      ]
    },

    {
      "type": "qemu",
      "name": "kvm",
      "vm_name": "{{ user `hostname` }}",

      "format": "qcow2",
      "accelerator": "kvm",
      "net_device": "virtio-net",
      "disk_interface": "virtio",
      "disk_size": 40960,
      "disk_compression": true,
      "iso_checksum": "{{ user `iso_checksum` }}",
      "iso_checksum_url": "{{ user `iso_checksum_url` }}",
      "iso_checksum_type":  "{{ user `iso_checksum_type` }}",
      "iso_url":  "{{ user `iso_url` }}",
      "http_directory": "{{ template_dir }}",
      "ssh_username": "op",
      "ssh_password": "op",
      "ssh_wait_timeout": "40m",
      "shutdown_command": "echo 'op' | sudo -S halt -p",
      "headless": "{{ user `headless` }}",
      "boot_wait": "5s",
      "boot_command": [
        "<esc><esc><enter><wait>",
        "/install/vmlinuz ",
        "auto-install/enable=true ",
        "auto url=http://{{ .HTTPIP }}:{{ .HTTPPort }}/{{ user `codename` }}.cfg ",
        "hostname={{ .Name }} ",
        "debconf/frontend=noninteractive ",
        "keyboard-configuration/modelcode=SKIP keyboard-configuration/layout=USA ",
        "keyboard-configuration/variant=USA console-setup/ask_detect=false ",
        "passwd/user-fullname=Op ",
        "passwd/username=op ",
        "passwd/user-password=op ",
        "passwd/user-password-again=op ",
        "initrd=/install/initrd.gz -- <enter>"
      ],
      "qemuargs": [
        ["-m", "1024M"],
        ["-smp", "1"  ]
      ]
    },

    {
      "type": "vmware-iso",
      "name": "vmware",
      "vm_name": "{{ user `hostname` }}",

      "guest_os_type": "debian8-64",
      "tools_upload_flavor": "linux",
      "disk_size": 40960,
      "iso_checksum": "{{ user `iso_checksum` }}",
      "iso_checksum_url": "{{ user `iso_checksum_url` }}",
      "iso_checksum_type":  "{{ user `iso_checksum_type` }}",
      "iso_url":  "{{ user `iso_url` }}",
      "http_directory": "{{ template_dir }}",
      "ssh_username": "op",
      "ssh_password": "op",
      "ssh_wait_timeout": "40m",
      "shutdown_command": "echo 'op' | sudo -S halt -p",
      "headless": "{{ user `headless` }}",
      "boot_wait": "5s",
      "boot_command": [
        "<esc><esc><enter><wait>",
        "/install/vmlinuz ",
        "auto-install/enable=true ",
        "auto url=http://{{ .HTTPIP }}:{{ .HTTPPort }}/{{ user `codename` }}.cfg ",
        "hostname={{ .Name }} ",
        "debconf/frontend=noninteractive ",
        "keyboard-configuration/modelcode=SKIP keyboard-configuration/layout=USA ",
        "keyboard-configuration/variant=USA console-setup/ask_detect=false ",
        "passwd/user-fullname=Op ",
        "passwd/username=op ",
        "passwd/user-password=op ",
        "passwd/user-password-again=op ",
        "initrd=/install/initrd.gz -- <enter>"
      ],
      "vmx_data": {
        "cpuid.coresPerSocket": "1",
        "ethernet0.pciSlotNumber": "32",
        "memsize": "1024",
        "numvcpus": "1"
      }
    },

    {
      "type": "lxc",
      "name": "lxc",
      "container_name": "{{ user `hostname` }}",

      "template_name": "download",
      "template_parameters": [
        "--dist", "ubuntu",
        "--release", "{{ user `codename` }}",
        "--arch", "{{ user `arch` }}"
      ],
      "attach_options": ["--clear-env"],
      "config_file": "/usr/share/lxc/config/ubuntu.common.conf"
    },

    {
      "type": "null",

      "ssh_host": "localhost",
      "ssh_username": "op",
      "ssh_password": "op",
      "ssh_port": 2222
    }
  ],

  "provisioners": [
    {
      "type": "shell",
      "except": ["lxc"],

      "execute_command": "echo op | {{ .Vars }} sudo -E -S bash {{ .Path }}",

      "script": "{{ user `medley_path` }}/{{ user `medley` }}.sh"
    },

    {
      "type": "shell",
      "only": ["lxc"],

      "execute_command": "{{ .Vars }} bash {{ .Path }}",
      "environment_vars": ["DEBIAN_FRONTEND=noninteractive", "LC_ALL=C"],

      "inline": [
        "while ! ip route get 1.1.1.1 &>/dev/null; do sleep 0.1; done",
        "apt-get -y update",
        "apt-get -y install --no-install-recommends curl ca-certificates sudo ssh"
      ]
    },
    {
      "type": "shell",
      "only": ["lxc"],

      "execute_command": "{{ .Vars }} bash {{ .Path }}",
      "environment_vars": ["LC_ALL=C"],

      "script": "{{ user `medley_path` }}/{{ user `medley` }}.sh"
    }
  ],

  "post-processors": [
    {
      "type": "vagrant",

      "output": "{{ user `boxname` }}-{{ .BuildName }}.box",
      "vagrantfile_template": "{{ template_dir }}/template.rb"
    }
  ],

  "variables": {
    "boxname": "ubuntu-stable-minimal",
    "hostname": "minimal",
    "distribution": "ubuntu",
    "suite": "stable",
    "codename": "bionic",
    "arch": "amd64",
    "medley": "minimal",
    "medley_url": "https://scripts.omu.sh",
    "medley_path": "../../../srv/scripts",
    "headless": "true",

    "iso_checksum": "",
    "iso_checksum_type": "none",
    "iso_checksum_url": "",
    "iso_url": "none"
  }
}
